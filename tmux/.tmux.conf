# set -g default-terminal "tmux-256color"
# set -ag terminal-overrides ",xterm-255color:RGB"

##### --- OXOCARBON THEME START --- #####
# Status bar
set -g status on
set -g status-interval 5
set -g status-style "bg=#161616,fg=#dde1e6"

# Messages
set -g message-style "bg=#262626,fg=#f2f4f8"
set -g message-command-style "bg=#262626,fg=#33b1ff"

# Panes Dividers
set -g pane-border-style "fg=#393939"
set -g pane-active-border-style "fg=#be95ff"

# Copy mode
setw -g mode-style "bg=#393939,fg=#f2f4f8"

# Window list
setw -g window-status-separator ""
setw -g window-status-style "bg=#161616,fg=#525252"
setw -g window-status-current-style "bg=#262626,fg=#f2f4f8"

setw -g window-status-format "  #[fg=#525252]#I:#W  "
setw -g window-status-current-format "  #[fg=#ee5396]#I:#W  "

# Clock / bell
set -g clock-mode-colour "#33b1ff"
set -g bell-action any
set -g visual-bell on

# Status left/right
set -g status-left-length 40
set -g status-right-length 80

set -g status-left  "#[fg=#be95ff]  #S "
set -g status-right "#[fg=#525252]%Y-%m-%d #[fg=#33b1ff]%H:%M "
##### --- OXOCARBON THEME END --- #####

set -g allow-rename off
setw -g automatic-rename off

set -g prefix C-a
unbind C-b
bind-key C-a send-prefix

unbind %
bind | split-window -h

unbind '"'
bind - split-window -v

unbind r
bind r source-file ~/.tmux.conf

bind j resize-pane -D 5
bind k resize-pane -U 5
bind l resize-pane -R 5
bind h resize-pane -L 5

bind -r m resize-pane -Z

bind M-c attach-session -c "#{pane_current_path}"

set -g mouse on

set-window-option -g mode-keys vi

bind-key -T copy-mode-vi 'v' send -X begin-selection # start selecting text with "v"
bind-key -T copy-mode-vi 'y' send -X copy-selection # copy text with "y"

unbind -T copy-mode-vi MouseDragEnd1Pane # don't exit copy mode when dragging with mouse

# remove delay for exiting insert mode with ESC in Neovim
set -sg escape-time 10

# Inactive pane border color
set -g pane-border-style fg=colour0
# Active pane border color
set -g pane-active-border-style fg=colour8

# tpm plugin
set -g @plugin 'tmux-plugins/tpm'

# list of tmux plugins
set -g @plugin 'christoomey/vim-tmux-navigator'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'
